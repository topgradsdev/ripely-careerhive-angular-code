import { YoutubeVimeoUrlDirective } from './youtube-vimeo-url.directive';

describe('YoutubeVimeoUrlDirective', () => {
  it('should create an instance', () => {
    const directive = new YoutubeVimeoUrlDirective();
    expect(directive).toBeTruthy();
  });
});
